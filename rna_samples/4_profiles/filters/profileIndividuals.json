{
    "input": {
        "profileallDetailsResortRegistered": {
            "_eq": "{{registeredHotelId}}"
        },
        "profileallDetailsUpdateDate": {
            "_gte": "{{updateDate}}"
        },
        "reservationDetailsResort": {
            "_eq": "{{hotelId}}"
        }
    },
    "limit": {{limit}}
}